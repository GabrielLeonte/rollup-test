```
import { Service } from './dist';

const NotificationService = new Service();

const send = async () => {
  try {
    NotificationService.newReward(10, 'Mec');
  } catch (err) {
    console.error(err);
  }
};

send();
```